import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    margin: 20,
  },

  title: {
    color: "white",
    fontSize: 30,
    fontWeight: "bold",
  },
});

export default styles;
